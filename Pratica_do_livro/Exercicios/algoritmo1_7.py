n = float(input('Numero de canetas: '))
y = float(input('Nota de pagamento: '))
z = float(input('Quanto de troco? '))


if (z > y) (n > 0) (y >= 0) (z >0):
    calc = (y - z) / n;
    print(calc);
else:
    print('Erro: os valores são inconsistentes! ');
